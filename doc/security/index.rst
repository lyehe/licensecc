Security
========

These pages document the reviewed security boundary for the native runtime,
hosted services, data stores, and release process. They describe controls that
the repository can enforce and the separate controls a deployment operator
must prove.

.. toctree::
   :maxdepth: 2

   threat-model
