name: Cloudflare_License_Admin

on:
  push:
    paths:
      - ".github/workflows/cloudflare-license-admin.yml"
      - "services/cloudflare-license-admin/**"
      - "services/cloudflare-licensing-backend/migrations/**"
      - "services/cloudflare-licensing-backend/src/entitlements/**"
      - "doc/analysis/license-admin-vite-react*.rst"
  pull_request:
    paths:
      - ".github/workflows/cloudflare-license-admin.yml"
      - "services/cloudflare-license-admin/**"
      - "services/cloudflare-licensing-backend/migrations/**"
      - "services/cloudflare-licensing-backend/src/entitlements/**"
      - "doc/analysis/license-admin-vite-react*.rst"

permissions:
  contents: read

jobs:
  admin:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    defaults:
      run:
        working-directory: services/cloudflare-license-admin
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: services/cloudflare-license-admin/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Secret marker scan
        shell: bash
        working-directory: .
        run: |
          pem_begin='BEGIN'
          pem_scope='PRIVATE'
          pem_kind='KEY'
          pem_marker="${pem_begin} (RSA |EC |DSA |OPENSSH |ENCRYPTED )?${pem_scope} ${pem_kind}|${pem_begin} ${pem_scope} ${pem_kind}"
          cloudflare_token='CLOUDFLARE_API_TOKEN'
          admin_bearer='ADMIN_BEARER'
          jwt_like='[A-Za-z0-9_-]{40,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}'
          token_marker="${cloudflare_token}=|${admin_bearer}=|${jwt_like}"
          if grep -R -n -E --exclude-dir=node_modules --exclude-dir=.wrangler --exclude-dir=.online-key "$pem_marker" services/cloudflare-license-admin .github/workflows/cloudflare-license-admin.yml; then
            echo "PEM key marker found"
            exit 1
          fi
          if grep -R -n -E --exclude-dir=node_modules --exclude-dir=.wrangler --exclude-dir=.online-key "$token_marker" services/cloudflare-license-admin .github/workflows/cloudflare-license-admin.yml; then
            echo "secret-like token found"
            exit 1
          fi

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: SQL tests
        run: npm run test:sql

      - name: Dry-run deploy
        run: npm run dry-run

      - name: Apply local migrations
        run: npm run migrate:local
