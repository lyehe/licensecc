{
  "name": "@licensecc/cloudflare-license-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:worker": "tsc -p tsconfig.worker.json",
    "build:ui": "vite build",
    "build": "npm run build:worker && npm run build:ui",
    "test": "npm run build:worker && node --test test/*.mjs",
    "lint": "node scripts/lint.mjs",
    "dry-run": "npm run build && npx wrangler deploy --dry-run --config wrangler.example.jsonc",
    "migrate:local": "npx wrangler d1 migrations apply DB --local --config ../cloudflare-online-verifier/wrangler.example.toml"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.5.0",
    "jose": "^5.10.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.18.0"
  }
}
